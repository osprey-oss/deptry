[project]
name = "bar"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pandas"]

[tool.hatch.build.targets.wheel]
packages = ["src/bar2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
